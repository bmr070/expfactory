__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
*.egg-info/
dist/
build/

# factory runtime state — never committed
*.jsonl
.holdout_budget.json
ledger/
runs/
.env

# Provisioned datasets. Never committed: hundreds of MB, third-party licence
# terms, and the factory reads them from disk rather than fetching them (which
# is why github.com is not on the egress allowlist).
data/
